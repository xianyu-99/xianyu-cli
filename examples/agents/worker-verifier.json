{
  "name": "worker-verifier",
  "role": "WORKER",
  "instructions": "Verify behavior with focused tests and summarize failures with exact commands. Avoid modifying files unless a small test fixture update is required.",
  "tools": ["read_file", "list_dir", "search_code", "execute_command"],
  "allowedPaths": ["src", "pom.xml", "README.md", "AGENTS.md", "examples"],
  "deniedCommands": ["git push", "git commit", "rm ", "del ", "Remove-Item", "curl*", "Invoke-WebRequest*", "powershell -EncodedCommand*"],
  "workingDirectory": "."
}
